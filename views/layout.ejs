<!DOCTYPE html>
<html>
  <head>
    <title>SilentAuction</title>

  </head>
<body>
<paper-toolbar>
  <div class='title'><%= link_to "Silent Auction", root_path %></div>
  <% if !admin_signed_in? %>
    <%= link_to "<paper-button>Log in</paper-button>".html_safe, new_admin_session_path() %>
    <%= link_to "<paper-button>Sign up</paper-button>".html_safe, new_admin_registration_path() %>
  <% else %>
    <%= link_to "<paper-button>Log out</paper-button>".html_safe, destroy_admin_session_path(), method: :delete %>
  <% end %>
</paper-toolbar>

<div class="flashes">
  <% if notice %>
    <p class="notice"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="alert"><%= alert %></p>
  <% end %>
</div>
<div class="wrapper">
  <%= yield %>
</div>
<div class="footer">
  Created with <%= image_tag("heart.png") %> by
  <a class="blue" href="http://fairviewhs.org/sites/nhs" target="_blank">Fairview National Honor Society</a> and
  <a class="blue" href="https://about.fairviewhs.org/" target="_blank">Fairview Web Team.</a>
</div>
</body>
</html>
